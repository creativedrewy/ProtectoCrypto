package com.creativedrewy.protectocrypto.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val ProtectoCryptoShapes = Shapes(
    // Small components: 16dp corners (matches XML SmallComponent)
    small = RoundedCornerShape(16.dp),
    // Medium components: 8dp corners (matches XML MediumComponent)
    medium = RoundedCornerShape(8.dp),
    // Large components: 32dp corners (matches XML LargeComponent)
    large = RoundedCornerShape(32.dp)
)
